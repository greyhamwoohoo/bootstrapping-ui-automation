## Start The-Internet locally in Docker
To run the app locally, from the infra/the-internet-docker-localhost folder:

```
docker-compose up
```

To access the application, navigate to:

```
http://localhost:9000
```
