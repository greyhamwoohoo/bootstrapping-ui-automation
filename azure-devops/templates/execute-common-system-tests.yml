parameters:
- name: relativeBaseFolder
  displayName: Relative path of the folder containing the version of the framework to build
  type: string
  default: src
  
steps:
- task: DotNetCoreCLI@2
  displayName: 'Run Common.SystemTests'
  inputs:
    command: test
    workingDirectory: $(System.DefaultWorkingDirectory)/${{ parameters.relativeBaseFolder }}/Yasf.Common.SystemTests/bin/$(Build.Configuration)/net6.0/publish
    arguments: Yasf.Common.SystemTests.dll
    publishTestResults: true
    testRunTitle: Common.SystemTests      
